{
  "name": "@flowcatalyst/workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ui-components",
    "control-plane"
  ],
  "scripts": {
    "dev": "bun run --filter control-plane dev",
    "build": "bun run --filter '*' build",
    "ui:dev": "bun run --filter ui-components dev",
    "control-plane:dev": "bun run --filter control-plane dev"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  }
}
