package tech.flowcatalyst.auth.service;

import jakarta.enterprise.context.ApplicationScoped;

/**
 * Service for role management.
 */
@ApplicationScoped
public class RoleService {

    // TODO: Implement role management logic
    // - createRole(name, description, permissions)
    // - updateRole(roleId, ...)
    // - deleteRole(roleId) - prevent deletion of system roles
    // - addPermission(roleId, resource, action)
    // - removePermission(roleId, permissionId)
}
