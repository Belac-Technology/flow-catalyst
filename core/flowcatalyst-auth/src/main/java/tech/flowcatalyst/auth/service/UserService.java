package tech.flowcatalyst.auth.service;

import jakarta.enterprise.context.ApplicationScoped;

/**
 * Service for user and service account management.
 */
@ApplicationScoped
public class UserService {

    // TODO: Implement user management logic
    // - createUser(email, password, tenantId)
    // - createServiceAccount(clientId, clientSecret, tenantId)
    // - updateUser(principalId, ...)
    // - deactivateUser(principalId)
    // - assignRole(principalId, roleId)
    // - removeRole(principalId, roleId)
}
